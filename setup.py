
from setuptools import setup, find_packages

setup(
    name='sshspawner_didd',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your library's dependencies here
    ],
)

